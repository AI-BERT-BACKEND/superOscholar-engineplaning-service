spring:
  security:
    user:
      name: test
      password: test
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

feign:
  task-service:
    url: http://localhost
  profile-service:
    url: http://localhost

jwt:
  secret: test-jwt-secret-for-tests-32-bytes