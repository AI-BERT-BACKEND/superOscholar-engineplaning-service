spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  security:
    user:
      name: test
      password: test

feign:
  task-service:
    url: http://localhost
  profile-service:
    url: http://localhost

jwt:
  secret: test-jwt-secret-for-tests-32-bytes